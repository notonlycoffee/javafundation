1.在JAVA环境下 ReentrantLock 和synchronized 都是 可重入锁

2.线程可以获得他已经拥有的锁，运行线程多次获得同一个锁，就是可以重入同步。这种情况通常是同步代码直接或者间接调用也包含了同步代码的方法，
	并且两个代码集都使用同一个锁。如果没有可重入同步，那么，同步代码就必须采取很多额外的预防措施避免线程阻塞自己。
	
3.java提供了一种同步机制，它不提供对所得独占访问，但童颜够可以确保对变量的每一个读取操作都返回最近写入的值，这种机制就是只用volatile变量。

4.